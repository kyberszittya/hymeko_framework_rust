# R2 — per-step state-dependent residual: contact-retention frontier advances (matched TD3-vs-SAC)

**2026-07-28 · branch `recovery/coin-r9-causal-residual-delivery` · worktree `hymeko_coin_r9_wt` · dev s1 (14250) · s4/s7 untouched · f1–f4 SEALED · teacher-free RL · NO further RL beyond this run**

## Summary

R1 showed a constant residual only adds coast momentum. R2 promotes it to a **per-step state-dependent** residual
`δ_ψ(o_t, h_clone,t, δu_{t-1})` — conditioned on the FROZEN clone's GRU hidden state + the previous residual (no second
recurrent net) — trained by a matched TD3-vs-SAC per-step loop. Result:

- **R2-A `RESIDUAL_STATE_DEPENDENCE_PASS`** — the residual is genuinely per-step state-dependent (std 0.975; residual dimensions
  correlate **up to 0.95** with contact force / coin geometry / forward velocity), not a constant.
- **R2-B `CONTACT_RETENTION_FRONTIER_ADVANCES`** — the learned residual **holds light contact ~11 mm longer**: contact-exit
  advances from the clone's 61.4 mm to **50.66 mm** (TD3), with **no clamp and no stall**. min_dtz 39.58 mm.
- **R3 / R4 not reached** — the contact frontier is at ~50 mm, not yet the 20–30 mm corridor or K6.

Per the pre-registered stop condition, contact pushed below ~50 mm with no K6 means the system is **on the correct mechanical
front** — the next round is *the same RL branch with more budget* (push the frontier to the corridor), **not** an action-basis
expansion. **Stopped here.** Verdict: **`CONTACT_RETENTION_FRONTIER_ADVANCES`** (R2-A + R2-B both pass).

## R0/R2 update-zero identity

The temporal wrapper preserves the identity: a zero-initialised per-step residual reproduces the K2 clone **bit-for-bit** on
the full frozen chain (coin trace identical, 46.20 = 46.20 mm). Warm-started to the clone (distil residual→0 over the clone
trajectory) so training begins at the clone.

## R2 — matched TD3-vs-SAC (`coin_kinetic_r2_rl.py`; 600 options/algo; wall 73 s; RSS 0.32 GB)

Contract: per-step residual over the augmented state `[norm(41-D obs) · clone GRU hidden (64) · prev residual (4)] = 109-D`;
α = 0.15; task-tied light-contact-following reward; best checkpoint by the **lexicographic champion** (safety ≻ K6 ≻ close+moving
release ≻ contact-exit dtz ≻ landing ≻ smoothness) — not the Q-value or the raw reward; staged eval every 30 options; identical
env / reward / snapshot / budget / net / seed, only the algo differs.

| algo | min_dtz | contact-exit | released | K6 | safe | residual std | max state-corr |
|---|---|---|---|---|---|---|---|
| **TD3** (main) | **39.58 mm** | **50.66 mm** | False | False | ✅ | 0.975 | **0.95** (fn) / 0.82 (dtz) / 0.84 (v_par) |
| SAC (comparator) | 40.89 mm | 57.12 mm | False | False | ✅ | 0.323 | 0.95 (v_par) / 0.91 (dtz/fn) |

**TD3 ≫ SAC** (contact-exit 50.66 vs 57.12 mm), consistent with the prior arc. Best checkpoint kept (TD3 it 300, exit 50.66 mm),
**not** the last (it 600 regressed to exit 60.85 mm) — the champion selection worked. Milestone @240: TD3 already exit 53.7 mm.

## The event trace — the state-dependent coin-following skill

Best-TD3 KINETIC transport (light contact held to the frontier):

| step | dtz mm | v_par | fn (min) | |
|---|---|---|---|---|
| 1 | 77.99 | 0.194 | 2.68 | clone takes over |
| 3 | 72.28 | 0.343 | 0.57 | light contact |
| 6 | 61.00 | 0.370 | 0.31 | **clone would exit here** |
| 7 | 57.30 | 0.357 | 0.22 | **residual holds contact** |
| 8 | 53.84 | 0.324 | 0.12 | still following |
| 9 | 50.66 | 0.304 | **0.068** | frontier — coast to 39.58 mm |

The clone loses bilateral contact at ~61 mm (6 steps); the per-step residual **extends the light-contact follow to 9 steps —
down to 50.66 mm** — keeping the normal force light (0.07–0.83 N, never a clamp) and v_par positive throughout (0.19→0.38→0.30).
This is exactly the state-dependent skill R2 targeted: actively follow the sliding coin in light contact past the hand/clone
wall, *without* re-clamping. The landing (39.58 mm) is close to R1's (39.56 mm) because both eventually coast — but R2 advanced
the mechanistically-decisive **contact frontier** (50.66 vs 60 mm), the prerequisite for a close-and-moving release (R3).

Reward decomposition (TD3): follow +9.42 (light-contact progress), slow −0.79, clamp 0, neg 0, reversal 0; terminal −20.66
(state-dependent contact-loss for exiting at 50.66 mm > the 30 mm corridor). Safety held throughout (peak_qdot ≤ 3, coin
speed ≤ 1.5).

## Honest reading

The TD3 residual maxes one action dimension (a constant forward push, like R1) and **state-modulates the others** (they
correlate 0.82–0.95 with the contact/geometry/velocity) — that state-dependent modulation is what advances the contact
frontier; a constant residual (R1) could not (it exited at 60 mm). The landing is coast-limited from wherever contact is lost,
so it barely improves over R1; the *win* is the contact frontier, which R3 must push to ≤ 30 mm to enable a delivering
close-and-moving release.

## Gate ladder

- R2-A `RESIDUAL_STATE_DEPENDENCE_PASS` — **done** (std 0.975, corr ≤ 0.95).
- R2-B `CONTACT_RETENTION_FRONTIER_ADVANCES` — **done** (exit 50.66 mm < 55 mm, no clamp/stall).
- R3 `CLOSE_MOVING_RELEASE_MANIFOLD_REACHED` (min_dtz ≤ 30 mm, +v_par, light, no clamp/stall) — **next**: continue the same
  RL branch with more budget to push the frontier from ~50 mm to the 20–30 mm corridor and release close-and-moving.
- R4 `FIRST_LEARNED_S1_K6_DELIVERY` — strict K6, teacher-free.

Stop-condition check: contact reached ~50 mm (not stuck at 60), so the residual action basis **does** give coin-following
authority — continue the same TD3 branch (SAC as comparator), no action-basis expansion needed.

## Files touched (all new / additive; K0–R1 modules untouched except an additive hook)

| file | role |
|---|---|
| `hymeko_rl/coin_delivery/theta_option/kinetic_residual2.py` | `TemporalResidualPolicy`, `KineticTemporalResidualController` (per-step δ over augmented state), residual_fn factories |
| `hymeko_rl/coin_delivery/theta_option/kinetic_rl2.py` | state-dependent `reward2`, `collect_episode`, compact matched per-step TD3/SAC (`train_perstep`), lexicographic champion |
| `hymeko_rl/experiments/coin_kinetic_r2_rl.py` | R2 driver (update-zero, distil warm-start, matched 600, R2-A/R2-B/R3/R4 gates, event trace) |
| `hymeko_rl/coin_delivery/theta_option/kinetic_clone.py` | +`CloneActor.hidden_vec()` (expose the frozen clone hidden — additive) |
| `hymeko_rl/tests/test_coin_kinetic_contract.py` | +2 tests (reward2 light-contact/state-dependent-loss; temporal update-zero identity) |
| `reports/2026-07-28-coin-r9-r2-rl/r2_rl.json` | metrics + traces (md5 `3397cecf2c59ab22`) |

**Reuse:** `option_rl.agents` (`make_actor`, `DetActor`, `GaussActor`, `QNet`, `_polyak`), `residual_option_env.distill_zero_residual`.

## Tests / static analysis

- `pytest test_coin_kinetic_contract.py` — **17 passed** (15 prior + the 2 R2 tests).
- `ruff check` clean; `radon cc -a` A (3.17). The `hidden_vec` addition is additive (K2 tests green).

## Provenance

Git `3e84de83` (R1 commit; R2 files uncommitted at run time). Python 3.11.15 / mujoco 3.10.0 / numpy 2.4.6 / torch 2.12.0 /
macOS-26.5.2-arm64. Seeds: cradle 14250; RL seed 0. Residual bound α 0.15. AUG_DIM 109. Peak RSS 0.32 GB. Deterministic
(physics + policy reproduce; wall-time is a measurement).

## Recommendation (short stop — awaiting your green)

R2-A + R2-B pass: the per-step residual is genuinely state-dependent and **advances the contact-retention frontier from 61 to
50.66 mm** (teacher-free, safe, no clamp/stall) — the coin-following skill R1's constant residual lacked. The system is on the
correct mechanical front; **R3 = continue the same TD3/SAC branch with more interaction budget** to push the frontier to the
20–30 mm corridor and release close-and-moving, then K6 (R4). **No further RL started.**
