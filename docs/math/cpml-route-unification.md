# CPML route topology — mathematics pointer

The **updated equations** and the **Highway / Capsule / KAN unification** narrative for **`topology="route"`** vs **`pyramid`** live in the mdBook:

**[CPML tiers as routes: Highway, Capsule, and KAN views →](../book/src/research/cpml-routing-highway-capsule-kan.md)**

Implementation: `signedkan_wip/src/cpml.py` (`CPML`, `CPMLConfig`: `topology`, `tier_organization`, `capsule_route_hidden`), Gömb `GombConfig.cpml_*` in `signedkan_wip/src/hymeko_gomb/cascade.py`.

Task report (acceptance + file list): `reports/2026-05-12-cpml-route-unification-highway-capsnet-kan.md`.
