---
name: project-gomb-hsikan-fsr-llm
description: "New LLM architecture (Gömb/HSiKAN/Fiber-Spike-Rotor on Chebyshev-CR) — planned 2026-06-29, PARKED at plan, do not implement until user says go"
metadata: 
  node_type: memory
  type: project
  originSessionId: aa3b2370-d3d6-4612-b217-1dbd327c115c
---

2026-06-29 (user "initiate"): a new autoregressive **LM architecture** composing existing HyMeKo primitives. Sequence-mixer replaces softmax attention with a signed, rotor-transported, spike-gated **walk-holonomy** over a causal token graph (= `structural_actor` signed B^L lifted from Z₂ to a non-abelian rotor connection; **rotor generalises RoPE**, spikes give content-dependent sub-O(T²) routing); channel-mixer = HSiKAN on the **Chebyshev/CR** cell (train-CR / deploy-Chebyshev, GPU-friendly); residual stream on the hypersphere (**Gömb**) + Gömb-strict leakage audit as eval discipline.

**Status: Phase 0 DONE 2026-06-29** (user said "start the toy"). Plan: `docs/plans/2026-06-29-gomb-hsikan-fsr-llm/`; report `reports/2026-06-29-fsr-lm-phase0.md`. Package `hymeko_lm/` BUILT (composition over `signed_kan`, `signedkan_wip.src.embeddings.cayley_rotor`, `hymeko_rl.structural_actor`; **CORE untouched, no new dep**; zero existing transformer code = clean slate). FSR sequence mixer (`sequence_mixer.py`) **VALIDATED** on the fixed-lag-copy discriminator: converges to the irreducible floor (0.42 vs floor 0.35 vs uniform 2.77) → offset-routing works. 15 tests pass, ruff+mypy clean, smoke 66 MB GPU. Key facts learned: (1) `cr_cheby`/`ChebyshevCRActivation` ALREADY EXISTS in `signed_kan/splines.py` (no new basis code — the planned additive hunk was unnecessary); (2) rotor import path = `signedkan_wip.src.embeddings.cayley_rotor`; `quat_rotate` expands v→u so the rotor must carry the batch dim; (3) gate must normalize by routing-mass not neighbour-count, and needs a learned per-offset positional bias (content-only QK can't route by offset). **MEMORY result (same session, 2026-06-29):** added associative-recall task (`data.make_associative_recall_batch`, per-seq-random k→v map) + `baselines.py` `CausalTransformerLM` (matched control). FSR FIRST FAILED recall (plateau 2.07≈uniform 2.77; xfmr→0.000 by step500). Hypothesis "gate sharpness" FALSIFIED (softmax≈sigmoid, both ~2.07 — `GateMode` kept as config axis). **Root cause = NO VALUE PROJECTION**: mixer transported raw hidden `R·h_j` not a learned `W_V` fiber → can't form the 2-layer induction circuit. **FIXED: added `to_v` value fiber (rotor transports the value fiber — truer to gauge story) → FSR now SOLVES recall (0.025 vs xfmr 0.000, both ~35k params)**, slower-to-induct than attention (~step1250 vs 500). 18 tests pass, ruff+mypy clean. Figures `reports/2026-06-29-fsr-lm-{phase0-losscurve,recall}.png`. **Design lesson: the FSR mixer needs Q/K(gate)+V(value-fiber) separation like attention; novelty stays in signed+rotor-connection+spike+sphere, NOT in dropping V.** **PHASE 1 PoC DONE 2026-06-29** (`reports/2026-06-29-fsr-lm-phase1.md`): byte-level TinyShakespeare (`text_data.py` ByteCorpus + `phase1_ab.py` harness `--mode smoke|full`) vs matched ~90k causal transformer. FIRST a NO-GO: FSR 4.845 ≈ corpus unigram entropy 4.779 → learned ZERO context (while recall/copy toys worked). DIAGNOSED (not declared): the Gömb sphere residual renormalised every sublayer with NO learnable readout scale → head reads unit vectors → can't sharpen logits past unigram. FIXED: added `_ScaledNorm` (nGPT-style learnable readout scale) + `ResidualMode` config axis (SPHERE vs PRENORM) + `SphereEmbedding(normalize=)`. **DEFINITIVE 3-seed/1500-step/seq128 (matched ~368k params): FSR-prenorm 2.479 (seeds 2.453–2.495) BEATS transformer 2.570 (2.563–2.607) — disjoint ranges; FSR-sphere(Gömb) 2.770 LOSES.** So GO for FSR-prenorm; **Gömb sphere does NOT earn its place on this task** (loses even WITH the readout-scale fix — at 700 steps it was marginally ahead but transformer gains more with training). The win is carried by the signed-rotor-spike MIXER + HSiKAN cr_cheby cell, with a STANDARD pre-norm residual. Lesson: a normalised residual needs a learnable readout scale (`_ScaledNorm`); but sphere itself isn't the winner here — don't oversell Gömb. HONEST COSTS: 3.1× slower/token (dense O(T²·blocks) quat-rotate → hard top-k spike-sparsity is the lever), tiny scale, 1MB corpus. 18 tests pass. Arch diagrams + report on disk (`reports/2026-06-29-fsr-lm-{phase1.md,arch-model,arch-mixer,phase1.png}`). **SPEED lever DONE 2026-06-29** (`reports/2026-06-29-fsr-lm-speed.md`): hard top-k spike gate = `spike_k` config + `_sparse_mix` (O(T·k), gathers top-k V/rotor/sign then quat_rotate only k) vs `_dense_mix` (O(T²)). Causal (future→-inf, never selected). FINDINGS: (1) **quality preserved/improved** — k=16 bpb 2.60 < dense 2.65 < xfmr 2.77 (hard gate regularises); (2) **O(T²) wall fixed** — sparse/dense throughput speedup 1.05×@T128 → 2.5×@T256 → **25×@T512** → **dense OOMs@T1024 while sparse runs**; (3) **absolute speed still 3–7× behind transformer** because xfmr uses FUSED flash-attn (SDPA) and our mixer is EAGER pytorch (topk+gather+quat_rotate) — next lever = Triton/CUDA fused kernel (ENGINEERING not algorithm). Default k=16 good operating point. 21 tests. **FUSED-KERNEL INVESTIGATION 2026-06-29/30** (`reports/2026-06-29-fsr-lm-speed.md` update): torch.compile inductor WORKS on this box (old "skip-Windows" note stale) but gave only **+3%** — NOT launch-overhead-bound. Profiled (T=512): forward cheap (~2.7ms), step 25ms because **backward dominated by `aten::_index_put_impl_` = 82% CUDA** (atomic scatter-add of the data-dependent gathers; split V-gather + rotor/sign-gather, detaching rotor/sign saves 26%). Triton DOES compile from a real .py file here (viable). Real fix = flash-style fwd+bwd Triton kernel w/ manual segmented backward (multi-hour, risky) — NOT started, put to user. **USER INSIGHT 2026-06-30: this GPU atomic-scatter penalty IS the rationale for NAGARE** (the dataflow/HSMM/FPGA substrate removes it by construction: spikes=event-firing, rotor=Toeplitz/systolic, walk-holonomy=streaming reduce). Added a rationale section to `docs/plans/2026-06-18-rotor-embedded-nagare/plan.{tex,pdf}` — the FSR mixer is a 2nd, larger Nagare client (shares signed-scatter + Cayley-rotor ops). See [[project-gsphf-attractor-planning-integration]] / Nagare-HSMM line. **LM IMPLEMENTATION MATURED 2026-06-30**: added `model.generate` (temp/top-k, seedable), `checkpoint.py` (config+weights round-trip), `train.py` (warmup+cosine LR, periodic val-bpb, ckpt/resume, sample gen; presets smoke/base/scale; default=prenorm+spike_k=16). 24 tests. **SCALE-UP run DONE** (`reports/runs/scale/`, fig `reports/2026-06-30-fsr-lm-scaleup.png`): 1.19M params, seq256, 3000 steps → **val bpb 2.223** (vs base 368k=2.479), 23min/2.0GB, produces COHERENT Shakespeare dialogue w/ character names (MENENIUS/SICINIUS). Architecture scales + samples legibly. **CAVEAT: did NOT re-run a matched transformer at 1.19M/seq256 — the vs-transformer MARGIN at scale is still unconfirmed** (Phase-1 margin was at 368k/seq128). **NAGARE-FOR-CODEX brief DONE** `docs/plans/2026-06-30-nagare-fsr-kernel-codex/CODEX_BRIEF.md` (closed-form fwd/bwd Rust kernels, 5 tasks→parity_gate; gaps: no Rust quaternion code exists yet, signed_scatter new). **NEXT = matched-transformer A/B AT SCALE (confirm margin); Gömb ablation; Chebyshev deploy parity; (Codex builds Nagare kernel).**

Staged + falsifiable: Ph0 skeleton+interfaces+smoke (no training); **Ph1 = go/no-go** tiny byte-LM (enwik8 default) vs matched-param transformer on val bits-per-byte + tokens/s + params (≥3 seeds) → lose = halt+report negative, don't scale; Ph2 ablations as classes not flags; Ph3 scale (separate plan).

**Novelty (bounded search 2026-06-29):** KAN-as-FFN is crowded (KAT/ChebyKAN/PolyKAN — Chebyshev is the *enabler not the moat*); the signed-Z₂-holonomy + rotor-RoPE-generalisation + spike-sparsity synthesis as an LM = "none found". Honest bet, not foregone. Builds on [[project-gauge-holonomy-signed-hsikan]], [[project-structural-actor-walk-holonomy]], [[project-hsikan-launchbound-alternatives]] (CR-Chebyshev), [[project-unify-hsikan-core]].
