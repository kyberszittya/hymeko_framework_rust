---
name: project-hsikan-geometric-attention-berge
description: "HSiKAN leakage-free rotor signed-link line. ARC (2026-06-17→18): 4 readout levers flat → INPUT-bounded → slerp rotor propagation (first lift +0.0045/+0.0105). Real bilinear best head. PROTOCOL-MATCHED: '0.04 gap' was a dedup artifact (we BEAT sigat_rotor; ceiling = pure SiGAT +0.036/+0.016). Adaptive prop = parity. INPUT ENRICHMENT: new k-walk A^k profile lifts NON-propagated baseline (otc +0.0168) but REDUNDANT with slerp. INDUCTIVE TRANSFER: rotor transfers cross-graph (0.81–0.86 on unseen graph) where transductive CAN'T — distinctiveness validated; learned increment modest (+0.04–0.06, random-init control below chance). Gap to pure SiGAT = expressivity. Arc is paper-shaped."
metadata: 
  node_type: memory
  type: project
  originSessionId: aeb7c69e-6ca1-48ad-a5bb-2ee322fb1764
---

**Status as of 2026-06-17 ~04:20 (paused for sleep; resume tomorrow).**

Goal: close the ~0.04 leakage-free AUROC gap to SiGAT (alpha 0.884 / otc 0.902) on rotor-HSiKAN signed-link prediction. The gap is "partly attention, not cycles" — so the lever is a stronger attention *readout*.

**DONE today:**
1. Training-knob A/B (wd=1e-4, grad-clip=1.0, class-weighted BCE, early-stop) wired into `run_hsikan_rotor.py` via `TrainConfig`. Result: small honest lift (+0.004 alpha / +0.005 otc), gate-clean, does NOT close the SiGAT gap. Report `reports/2026-06-17-hsikan-rotor-sota-levers.md`.
2. **Geometric attention head IMPLEMENTED**: new `signedkan_wip/src/core/geometric_triad_attention.py` — `GeometricTriadAttentionPool`: pools per-triad embeddings into per-vertex embeddings by signed attention, score = learned-gate mix of a **quaternion** (Hamilton real part, sig (+,-,-,-)) and a **Clifford** (Cl(2,0) geometric-product scalar, sig (+,+,+,-)) channel over the same projected features; tanh signed weights; magnitude-normalized index_add scatter. Reuses `sequence/clifford.py::geometric_product`. Wired as `run_hsikan_rotor.py --head geom_attn` (`--geom-channel both|quaternion|clifford`, `--geom-temperature`, `--geom-replace`). Plan: `docs/plans/2026-06-17-geometric-attention-head/` (4 artifacts). Tests: `tests/test_geometric_triad_attention.py` + geom_attn case in `tests/test_hsikan_rotor.py` — 24 pass, ruff clean.

**KEY RESULT (seed 0, tuned recipe):**
- First cut REPLACED h_v with the triad pool → DISASTER: alpha 0.737 / otc 0.717 (−0.13/−0.17 vs bilinear), and alpha gate 0.61 (LEAK). Diagnosed: erases the rotor node signal + magnitude-norm encodes a degree channel.
- Fix = **residual** (`node = h_v + pool`, now default; `--geom-replace` ablates): recovers accuracy + gates PASS.
- **5-seed result is in** (`reports/hsikan_geom_attn_20260617.jsonl`, report `reports/2026-06-17-geometric-attention-head.md`): bilinear vs geom_attn — alpha 0.8455 vs 0.8470 (+0.0015, tie), otc 0.8685 vs **0.8518 (−0.0166, REGRESS)**. Gates 0.530/0.486 clean. **NEGATIVE: geom_attn does NOT beat bilinear; SiGAT gap not closed (otc gap widened to −0.050).** The head is correct + leakage-clean, the geometry-as-readout just adds nothing net so far.

**GATE INSPECTION DONE 2026-06-17 13:5x JST — the readout's "adds nothing" is SCORE COLLAPSE, decisively.** `reports/2026-06-17-geom-gate-inspection.md` + `reports/geom_gate_inspect_20260617.jsonl` (seed 0, both bitcoin, tuned recipe). Built `summarise_gate()` in `geometric_triad_attention.py` + a generic `on_trained` Observer hook on `run_hsikan_rotor.run()` (default None, fires only for geom_attn) + `experiments/runs/inspect_geom_gate.py`. 29 tests pass, ruff clean. **Measured:** 100% of signed weights dead (|w|<0.05) on both datasets; gate σ frozen at 0.500 (init, never moved); W_q/W_k stuck at 0.1× init scale; W_v grew to ~3.2; pool/h_v ≈ 0.14. **Inferred:** the geometric score collapsed to ~0 → the pool `Σw·v/Σ|w|` is an UNWEIGHTED MEAN of values ("attention" in name only); NOT residual swamp (pool/h_v non-vanishing). Cause: score born tiny (W_q,W_k ×0.1 init × 1/√8 scale × tanh) + value+residual path already fits loss → no gradient pressure to wake the score.

**This reframes the fork (was: "sign-aware values"). The dead component is the SCORE, not the value magnitude.** Evidence-based next levers to WAKE the score: (a) drop the 0.1× W_q/W_k init suppression OR add a learnable score scale (dynamic range); (b) inject triad sign σ into the SCORE/KEY path so attention votes by balance. Sign-aware VALUES (the old #1) still helps (σ-weighted value mean carries sign) but is secondary. This is a model change → needs its own plan dir.

**WOKEN READOUT DONE + FALSIFIED 2026-06-17 (user chose "wake it first, then Berge").** Plan `docs/plans/2026-06-17-geom-attention-wake-score/` (4 artifacts). Added 3 opt-in knobs to `GeometricTriadAttentionPool` (defaults = legacy, reproducible): `score_init_scale` (0.1 legacy / 1.0 woken, no init suppression), `learn_scale` (learnable log-scale on score), `sign_aware` (`w = b_t·σ(s̃)`: triad BALANCE sign b_t sets vote direction, σ(s̃) learned relevance; warm-starts as balance-signed value mean). b_t from train triads only → leakage-safe. 37 tests pass, ruff clean. Report `reports/2026-06-17-geom-attention-wake-score.md`, data `reports/woken_ab_20260617.jsonl` (5-seed) + `woken_smoke_20260617.jsonl`.

**5-SEED RESULT (woken vs bilinear, tuned recipe):** alpha 0.8455 vs 0.8455 (TIE, Δ−0.0000, 2/5 wins); otc 0.8563 vs 0.8685 (REGRESS −0.0122, 0/5 wins). Woken recovered part of the dead-legacy otc regression (0.8518→0.8563) and IS alive + leakage-clean (gate 0.532/0.504 ≈ chance). **But it does NOT beat bilinear. The geometric-attention READOUT line is FALSIFIED across two generations (dead score → woken score), both negative.** Geometry-as-readout on simple triads is closed; the woken machinery is kept (defaults off) for reuse on a richer neighborhood.

**ROTOR-RELATIVE PROJECTION DONE + FALSIFIED 2026-06-17 (user steer: "the rotation must be projected, that's what bilinear does; we're working with complex numbers").** Plan `docs/plans/2026-06-17-rotor-relative-projection/` (4 artifacts). KEY MECHANISM found: bilinear `γ h_u^T W h_v` IS a learned projection of the relative rotation `R_u^T R_v`; but `CayleyRotorEmbedding` returns only `e=R·v0` (1 ref/block) → rotation about v0 fixes v0 → that DoF is DISCARDED before bilinear. Built (all kept, defaults off): `quat_mul`/`quat_conjugate`/`rotors()` accessor/`n_refs` in `cayley_rotor.py`; `core/rotor_relative_head.py` (`RotorRelativeHead`: projects relative rotor `conj(q_u)⊗q_v`, added to bilinear, dead-start γ); `run_hsikan_rotor.py --head rotor_rel` + `--n-refs`. 60 tests pass, ruff clean. Report `reports/2026-06-17-rotor-relative-projection.md`, data `rotorrel_ab_20260617.jsonl`+`rotorrel_smoke_20260617.jsonl`.

**5-SEED RESULT: FLAT.** Route A (rotor_rel) vs bilinear: alpha +0.0002, otc +0.0000 (9/10 seeds BIT-IDENTICAL). Route B (n_refs=2): −0.0051 alpha / +0.0005 otc. Gates 0.52/0.52 ≈ chance (clean). **Discriminator (not a bug):** rotors NON-degenerate (std 0.18 nodes / 0.24 edges); forced γ=1.0 STILL flat (±0.0004) → the relative-rotor term is LIVE but carries NO sign signal beyond bilinear. **Verified: the discarded about-v0 DoF is NOT sign-predictive; bilinear already captures the sign-relevant rotor projection.** Quaternion SO(3) ⊃ complex SO(2), so a complex variant can't carry more — negative generalizes.

**THREE comparison/readout-side levers now falsified** (geom_attn dead score → woken score → rotor-relative projection), all leakage-clean. Gap is localized AWAY from the node-endpoint comparison → toward neighborhood AGGREGATION (what SiGAT does).

**USER META-HYPOTHESIS (2026-06-17, flag for revisit):** Dr. Hajdu accepts the rotor-relative negative but believes "something fundamental is missing" — i.e. the three negatives may share a deeper common cause rather than being independent dead-ends. Hold this open: before declaring Berge the answer too, watch for a root cause that would explain why every comparison/readout lever lands flat (candidates to probe: the strict train-only triad construction throwing away signal; the rotor embedding's expressivity ceiling; the eval/dedup protocol; class imbalance interacting with the metric). Don't over-fit to "Berge will fix it."

**BERGE / k-CYCLE DONE + FLAT 2026-06-17 (user: "go with Berge now").** KEY DISCOVERY (Explore sweep): the closed-signed-cycle enumerator ALREADY EXISTS — `signedkan_wip/src/core/n_tuples.py::construct_k_arrays(g,k,max_cycles,seed)` → Rust `enumerate_k_cycles_rs` → `hymeko_graph::cycle_enum`, Davis-balance classify; `build_vertex_triad_incidence`+`encode_triads` already arity-agnostic. A new hymeko_hre Berge enumerator would have DUPLICATED it (berge.rs is open traversal, not cycle enum). So Berge = tiny wiring, NOT a Rust build. Built: `run_hsikan_rotor.py --cycle-k K --max-cycles N` branch (rotor × k>3, the combo never crossed — existing k-tuple runners use the leaky table embed). 22 tests pass. Plan `docs/plans/2026-06-17-berge-kcycle-rotor/`, report `reports/2026-06-17-berge-kcycle-rotor.md`, data `kcycle_ab_20260617.jsonl`+`kcycle_smoke`.

**5-SEED RESULT: FLAT.** k=3 vs k=4 (matched 20k cap): alpha 0.8457 vs 0.8450 (−0.0007), otc 0.8689 vs 0.8691 (+0.0002). Both |Δ|<0.001 (noise). Gates 0.52 clean. k=3-via-construct_k ≈ construct baseline (path verified). Richer neighborhoods DON'T help.

**FOUR INDEPENDENT LEVERS NOW FLAT** (geom dead score, woken score, rotor-relative projection, k=4 cycles) — all leakage-clean, all ~0.846/0.869, ~0.04 short of SiGAT every time. The gap is a COMMON ROOT CAUSE, not a per-lever deficiency. Dr. Hajdu's "something fundamental is missing" is now strongly evidenced.

**ROOT CAUSE FOUND 2026-06-17 (the payoff of the 4 negatives).** TWO findings:

**(1) THE BOTTLENECK IS THE INPUT, not the readout.** `signedkan_wip/src/baselines/cayley_rotor_baseline.py::_structural_features` → `STRUCT_DIM = 6`: the rotor's ENTIRE per-node input is 6 DEGREE-ONLY numbers (log-deg, log-pos-deg, log-neg-deg, pos-frac, neg-frac, signed-balance). No neighborhood / no who-you-connect-to. MEASURED: `val_auroc` pinned at ~0.857 alpha / ~0.876 otc across ALL FOUR levers (woken DROPS otc val; n_refs2 DROPS it; rotor_rel/k=4 identical) — more readout/neighborhood capacity does NOT raise the fit. INFERRED: all four levers are downstream of a fixed degree-only node feature; nodes with equal degree profiles are indistinguishable. SiGAT's real edge = message PROPAGATION over the signed graph (node sees its neighbors), which this line's NODE feature never does (triad pool only injects structure at readout, over the degree-only base). **The untested axis = enrich the INPUT: leakage-free neighborhood-aware features / signed propagation of the structural features before the rotor.** THIS is "something fundamental is missing" (user, confirmed).

**(2) The SiGAT target was a PROTOCOL MISMATCH.** "SiGAT 0.884/0.902" = the `sigat_rotor` model (SiGAT attn + rotor feats) from `reports/sigat_rotor_verify_20260616.jsonl`, measured in `run_baseline_audit` which is STRICT+shuffle-gated (so NOT the transductive-leaky published SiGAT) BUT has **no `--dedup`** — measured on the NON-deduped (easier) test set. The HSiKAN rotor line (0.846/0.869) is DEDUPED (harder true-held-out). Same-harness numbers (all strict+gated, non-dedup, 5-seed): cayley_rotor 0.867/0.895, sigat_rotor 0.880/0.901, pure sigat 0.906/0.910. So the like-for-like gap is smaller and confounded by dedup; the ~0.04 was cross-protocol. Re-measure the gap protocol-matched.

**FIX FOUND + WORKING 2026-06-17 (graphics×AI unification): SIGNED SLERP/NLERP ROTOR PROPAGATION on the INPUT.** User steer: shader interpolation = same problem (triad=triangle→barycentric; quaternion→slerp; linear pooling shrinks off the sphere = "interpolate normals then renormalize"). Built (plan `docs/plans/2026-06-17-signed-rotor-slerp-propagation/`, 4 artifacts): `embeddings/signed_rotor_propagation.py::SignedRotorPropagation` (signed nlerp aggregation on S³: `q_i' = normalize(w_self·q_i + (1/deg)Σ_j s_ij·align(q_j,q_i))`, hemisphere-aligned, edge-signed, K rounds, leakage-safe train-only); refactored `CayleyRotorEmbedding` into `rotors()`+`embed_rotors()`; wired `run_hsikan_rotor.py --rotor-prop-rounds K --rotor-prop-self-weight W` (RotorInjector applies prop between rotors and embed). Also added `tests/test_multidim_preservation.py` (guards propagation/pool never scalar-collapse) + `test_signed_rotor_propagation.py`. 49+ tests pass, ruff clean.

**SEED-0 RESULT (FIRST LEVER TO MOVE val):** baseline r0: alpha test 0.8636/val 0.8757, otc 0.8842/0.8844. sw=1 over-smooths (alpha drops, esp r3). **self_weight FIXES it**: r2 sw4 → alpha test 0.8664 (+0.003)/val held, otc test 0.8941 (+0.010)/val 0.9004 (+0.016). r1 sw2 → otc test 0.8940/val 0.9001. Gate 0.48-0.54 clean. otc deduped 0.894 now ~level with the NON-deduped sigat_rotor target (0.901) on the HARDER split. **5-seed A/B running** (`reports/rotorprop_ab_20260617.jsonl`: r0 vs r1sw2 vs r2sw4 + gate). Report `reports/rotorprop_smoke_20260617.jsonl`+`rotorprop_selfweight_20260617.jsonl`.

**PROPAGATION 5-SEED CONFIRMED (r2 sw4 winner):** alpha 0.8455→0.8500 (+0.0045), otc 0.8685→0.8790 (+0.0105), gates 0.53/0.52 clean. FIRST confirmed positive lever of the session, leakage-clean, BOTH datasets. `reports/rotorprop_ab_20260617.jsonl`. The win is the INPUT (manifold slerp propagation), bilinear KEPT as readout.

**HEAD ABLATION DONE (real→complex→quaternion→rotation), on the propagated input.** Plan `docs/plans/2026-06-17-link-head-ablation/` (4 artifacts). Built: `core/bilinear_head.py::ComplexDiagonalHead` (ComplEx: Re Σ w_k u_k conj(v_k), replaces bilinear), `core/rotor_relative_head.py::RotorGeodesicHead` (RotatE/geodesic: cos(θ/2)=⟨q_u,q_v⟩ per block→logit, adds to bilinear), `RotorInjector.propagated_rotors()` (quat/rotate now read PROPAGATED rotors — fixed latent bug where rotor_rel read un-propagated), `--head {complex,rotate}`. HOSVD/TuckER deliberately omitted (multi-relational tool; binary sign task has no relation mode → degenerates to bilinear; complex/quat ARE the algebraic weight-sharing the idea targets). 39 tests pass, ruff clean. `reports/headabl_smoke_20260617.jsonl` + 5-seed `headabl_ab_20260617.jsonl` (running).

**HEAD ABLATION DONE + REPORTED (fair test, 5-seed).** Dr. Hajdu flagged the in-pipeline ablation as confounded (ComplEx read REAL h_v; rotor heads additive→shared-grad corruption; rotate HURT). Confounded 5-seed masked everything to ~bilinear. Built the FAIR harness `run_rotor_head_ablation.py` (`RotorHeadModel`: all heads score the SAME propagated rotors q, standalone). **FAIR 5-seed: real > complex > geodesic > quat**, both datasets, gaps ~0.13 (≫ noise), NOT param artifact (complex & quat identical params, complex≫quat). alpha: real 0.8492 / complex 0.8334 / geodesic 0.7699 / quat 0.7161. otc: real 0.8741 / complex 0.8668 / geodesic 0.8063 / quat 0.7572. Gates 0.39/0.41 clean. **CONCLUSION: head algebra is NOT the gap — real bilinear is the best readout of q; structured algebra (complex/quat/geodesic) is worse. Expressivity > algebra at the readout; manifold structure pays off where you AGGREGATE (input/propagation), not where you COMPARE.** Side finding: real-on-q alone (0.849/0.874) ≈ full triad-encoder pipeline (0.850/0.879) → triad encoder adds little; candidate simplification. Reports: `reports/2026-06-17-signed-rotor-slerp-propagation.md` (the WIN) + `reports/2026-06-17-link-head-ablation.md` (this fair negative). Data: `rotorhead_fair_ab_20260617.jsonl`, `headabl_ab_` (confounded).

**PROTOCOL-MATCHED SiGAT DONE 2026-06-17 (item 2 below — the dedup artifact, confirmed).** `--dedup` added to `run_baseline_audit`; dedup single-sourced into `datasets.{undirected_pair,drop_train_pairs}` (run scripts now alias it). New driver `run_protocol_matched_sigat.py` + aggregator `eval/aggregate_protocol_match.py`. 5-seed grid (4 models × 2 graphs × 2 protocols, shuffle-gated). **RESULT (deduped, matched):** our line 0.850/0.879 BEATS sigat_rotor (0.833/0.868, +0.017/+0.012) — the "0.04 gap vs sigat_rotor" was ENTIRELY the dedup mismatch (sigat_rotor's 0.880/0.900 target was non-deduped). The real residual is vs **pure SiGAT** (deduped 0.886/0.895 → +0.036 alpha / +0.016 otc), smaller than alleged. Non-deduped leaks under shuffle (gate ⚠) → deduped is the honest protocol; pure SiGAT is the ceiling, NOT sigat_rotor. Report `reports/2026-06-17-protocol-matched-sigat.md`, data `protocol_matched_full.jsonl`.

**ADAPTIVE PROP DONE 2026-06-17 (parity):** learnable per-block self-retention `α_b=floor+exp(θ_b)` (log-space ≡ sigmoid residual gate; softplus is for HEADS not scale-free manifold mixing) — 5-seed parity with fixed sw=4 (removes the sw knob). Depth scan {2,3,4,6}: no gain (alpha over-smooths, otc flat). `reports/2026-06-17-adaptive-rotor-propagation.md`.

**INPUT ENRICHMENT DONE 2026-06-18 (mixed; the numbers lever, characterised).** Built `signedkan_wip/src/baselines/structural_features.py`: extensible leakage-safe `StructuralFeature` registry. **k-cycle participation ALREADY existed** (`_cycle_features`→cayley_rotor_{cyc,jump,jump_k4}) — reused. **NEW: exact k-walk signed `A^k` profile** (`walk_k3`: sparse `A^k·1` reach + ratio, cap-free; `core/walks.py` is a KAN constructor, NOT this) + clustering `ratios`. New baselines cayley_rotor_{walk,cyc_walk,full}; `run_hsikan_rotor --feature-spec`. **5-seed deduped gated:** walk lifts the NON-propagated audit baseline (cyc+walk otc +0.0168) but is PARITY on the slerp-propagated line (+0.003 otc) — slerp propagation IS signed-`A^k` aggregation, so the feature is redundant there. Seed-0 otc 0.9131 (beat pure SiGAT) DID NOT replicate (5-seed 0.882 — variance, not a win; lesson: large seed variance here, never report seed-0). `reports/2026-06-18-leakage-free-input-enrichment.md`. **Conclusion: the rotor line reaches ~0.879 otc by EITHER slerp OR walk-profile (don't stack); the residual to pure SiGAT (0.895) is expressivity (learned attention), not input.** Next is a direction decision (learned propagation weights → toward SiGAT, dilutes simplicity), not a quick win.

**INDUCTIVE TRANSFER DONE 2026-06-18 (nuanced positive — the line's RAISON D'ÊTRE validated).** New `run_inductive_transfer.py` (train weights on graph A, eval frozen on B's strict deduped split; reuses refactored `run_baseline_audit._train`). **Mechanism+discrimination WIN:** rotor transfers cross-graph (alpha↔otc, 0.81–0.86 AUROC on the UNSEEN graph); transductive `dadsgnn` **CANNOT** (`nn.Embedding(n_A)` can't index B → "cannot transfer"). **Gate confound caught:** naive shuffle-A gate fails (0.75–0.82, NOT chance — because eval uses B's REAL signed adjacency, so signed message-passing encodes B's signs regardless of A-training). Added **random-init control** = correct floor, sits BELOW chance (0.38–0.47). Decomposition: random-init (below chance) → any training warms net so B's real features express (0.75–0.82) → real-A adds learned increment **+0.038–0.063** (4/4 positive but ~1–1.5σ_shuffle — suggestive not strong). So most cross-graph AUROC is B's structural prior; the genuine learned-from-A transfer is modest. `reports/2026-06-18-inductive-transfer-test.md`. **The paper story: the rotor is the ONLY model here applicable to an unseen graph at all + a small consistent learned increment — a distinctiveness claim the AUROC race can't make.** Next: stronger transfer test (more seeds / slashdot-epinions pairs) OR node-holdout OR consolidate the arc into a paper.

**ROTOR GEOMETRY ABLATION DONE 2026-06-18 (foundational NEGATIVE — recontextualizes the whole line).** User asked "is the Cayley rotor utilized?" → built `MLPEmbedSignedModel` (identical proj/SGCN/classifier; rotor embedding swapped for a higher-capacity MLP of the same output dim, 16,661 ≥ 15,761 params — generous control). **5-seed deduped gated: rotor − MLP = ±0.002–0.005, all inside σ (0.01–0.027), both leakage-clean.** So the rotor's S³ GEOMETRY is NOT load-bearing on signed-link prediction — a plain MLP of the SAME features matches it. Combined with the head ablation (rotor ALGEBRA not load-bearing at readout), **neither the rotor geometry nor algebra does measurable work here.** KEY REFRAME: the line's validated wins (leakage-free, inductive, param-light, transfers cross-graph) come from **replacing the transductive nn.Embedding node-ID table with train-only STRUCTURAL FEATURES** — the MLP-embed control has all of them too. The rotor is one interchangeable embedding choice, NOT the active ingredient. Honest scope: "not load-bearing for signed-link with these feats+SGCN", not "rotor useless" (the ANN-index-projection use [[project-cayley-rotor-idea]] is untested here). `reports/2026-06-18-rotor-vs-mlp-embed-ablation.md`. **For the write-up: frame as structural-feature signed-link prediction; keep the rotor only for narrative/ANN tie-in (and say so), or default to the simpler MLP embed.**

**▶ RESUME HERE (2026-06-18, user → sleep). Pick ONE:**
1. **Harder-pair transfer grid** (queued, evidence-backed). Smoke: train bitcoin_otc → eval **slashdot** = 0.844 vs random-init 0.534, ~7s/cell (train-small/eval-large is cheap — eval is one forward). Run gated 5-seed: {cayley_rotor, cayley_rotor_walk} × source {bitcoin_alpha, bitcoin_otc} × target {slashdot, epinions} × arms {real, shuffle, randinit}, via `run_inductive_transfer.transfer_cell` (extend PAIRS or reuse the inline 3-arm pattern from `inductive_transfer_decomp_ab.jsonl`). Goal: push the learned increment past σ on a real distribution gap. slashdot 82k/549k pos0.77, epinions 132k/841k pos0.85 (cached, no network).
2. **Write up the reframed line** — paper-shaped: structural-feature, leakage-free, inductive signed-link prediction; honest (deduped) protocol; competitive w/ pure SiGAT; transfers cross-graph; rotor geometry NOT the active ingredient. Material = 7 reports (2026-06-17/18).
3. Node-holdout inductive (per-node support/target leakage subtlety) — defer.

Session state: working tree DIRTY, nothing committed this session; all new code gated (ruff clean, ~100 tests green across touched suites, mypy-strict clean on new modules; pre-existing untyped files noted in reports). BACKLOG.md ▶ NEXT STEP mirrors this.

**SESSION ARC (paper-worthy):** 4 readout/comparison levers flat → root cause = degree-only INPUT → fix = signed slerp manifold propagation (computer-graphics × signed-graph AI), first confirmed lift, leakage-clean → head ablation shows readout interchangeable post-fix. NEXT: confirm head 5-seed; write the propagation + ablation reports; consider learnable self_weight / per-block gate; the SiGAT "gap" was partly a non-deduped-target protocol mismatch (record in writeup). [[project-cayley-rotor-idea]] [[project-nature-leakage-paper]]

Existing pieces reused, NOT rebuilt: `core/attention.py` (signed-triad readout shape), `ac_hsikan/components/attention.py` (quaternion scoring), `sequence/clifford.py` (geometric product), `berge.rs`. CORE.YAML protects only `hymeko_core/` Rust. See [[project-cayley-rotor-idea]].
