\documentclass{article}
\usepackage[margin=1in]{geometry}
\title{G\"omb hyperparameter search (random)}
\author{ops}
\date{2026-05-12}
\begin{document}
\maketitle
\paragraph{Scope.}
Random search over learning rate, embedding width, outer bank count,
\texttt{topk}, tier count, Adam \texttt{weight\_decay}, optional
\texttt{pos\_weight} (train-set balance), and occasional mixed arity
\texttt{3,4} cycles.  Driver: \texttt{signedkan\_wip/src/run\_gomb\_tune.py}
subprocesses \texttt{run\_gomb\_smoke} with \texttt{80\_10\_10} split for
\texttt{test\_auroc}.  No new Python dependencies.

\paragraph{Rollback.}
Delete JSONL artifacts; remove script invocation; training defaults unchanged.

\end{document}
