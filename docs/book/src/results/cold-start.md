# Cold start (included from repo)

The following is the canonical **`COLD_START.md`** at the repository root (build, read-first list, artifact paths). **Edit that file**, not this wrapper, then rebuild the book.

{{#include ../../../../COLD_START.md}}
