[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "signedkan_native"
version = "0.1.0"
description = "Native-speed signed n-tuple enumeration for HSiKAN"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
