"""One-off **paper / table / profiling** scripts (moved from ``src/`` root).

Run as modules, e.g.:

    python -m signedkan_wip.src.paperkit.build_ledger
    python -m signedkan_wip.src.paperkit.analyze_paired_5seed …

See ``signedkan_wip/STRUCTURE.md`` for the full source layout map.
"""
